[project]
name = "25-thesis-vns"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mypy>=1.17.0",
]

[dependency-groups]
dev = [
    "ruff>=0.12.5",
]
